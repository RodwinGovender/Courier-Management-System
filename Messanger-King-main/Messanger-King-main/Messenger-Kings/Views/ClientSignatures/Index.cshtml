@model IEnumerable<Messenger_Kings.Models.ClientSignature>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Driver.Driver_IDNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.Driver_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sign_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MySignature)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Driver.Driver_IDNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.Driver_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sign_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MySignature)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Signaturee_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.Signaturee_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Signaturee_ID })
            </td>
        </tr>
    }

</table>


@section Scripts {
    <script src="~/Scripts/signature_pad.min.js"></script>
    <script src="~/Scripts/SignaturePadInit.js"></script>
}